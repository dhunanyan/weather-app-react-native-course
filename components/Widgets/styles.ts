import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  widgetContainer: {
    flex: 1,
    padding: 10,
    paddingBottom: 40,
  },
  squareWidgetContainer: {
    marginTop: 5,
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "center",
    gap: 5,
  },
});
