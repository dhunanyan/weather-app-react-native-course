import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  scrollView: {
    paddingLeft: 20,
    paddingTop: 20,
    paddingBottom: 10,
  },
  view: {
    flex: 1,
    flexDirection: "row",
    gap: 12,
  },
});
