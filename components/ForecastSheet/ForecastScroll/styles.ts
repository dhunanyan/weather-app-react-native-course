import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  scrollView: {
    paddingTop: 15,
    paddingBottom: 10,
  },
  view: {
    flex: 1,
    flexDirection: "row",
    gap: 12,
    paddingHorizontal: 20,
  },
});
